import { Container } from "react-bootstrap";

export default function NotAllowedPage() {
    return( 
        <Container fluid className="vh-auto">
            <h1 className="display-4 text-center"> U are not allowed. </h1>
        </Container>
    )
}